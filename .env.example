# Copy to .env.local in project root, then restart: npm run dev
# Dev server: http://localhost:3000

# Optional — signed-in workspace with cloud scan history
# Project Settings → API → Project URL (no /rest/v1) and anon public key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Analysis API — Hugging Face Space (no local backend required)
VITE_API_BASE_URL=https://cyto0plasm-cerebroai-backend.hf.space
