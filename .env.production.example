# Copy to .env.production for local production builds only.
# On Vercel, set VITE_API_BASE_URL in Project → Settings → Environment Variables instead.
VITE_API_BASE_URL=https://YOUR_USERNAME-cerebroai-backend.hf.space
